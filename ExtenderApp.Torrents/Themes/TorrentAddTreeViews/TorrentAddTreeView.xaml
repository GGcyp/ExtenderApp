<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:ExtenderApp.Torrents.Themes"
                    xmlns:view="clr-namespace:ExtenderApp.Views.Themes;assembly=ExtenderApp.Views"
                    xmlns:c="clr-namespace:ExtenderApp.Torrents.Converter">

    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="/ExtenderApp.Torrents;component/Themes/TorrentAddTreeItems/TorrentAddTreeItem.xaml"/>
    </ResourceDictionary.MergedDictionaries>

    <Style TargetType="{x:Type local:TorrentAddTreeView}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:TorrentAddTreeView}">
                    <Grid Background="{TemplateBinding Background}"
                          Grid.IsSharedSizeScope="True">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="30"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <!-- 表头行 -->
                        <Grid Grid.Row="0"
                              Background="{TemplateBinding Background}">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition
                                    Width="40"
                                    MaxWidth="40"
                                    SharedSizeGroup="Indent"/>
                                <ColumnDefinition
                                    Width="{Binding ActualWidth,
                                    RelativeSource={RelativeSource
                                    AncestorType={x:Type Grid},
                                    AncestorLevel=2},
                                    Converter={c:MaxWidthConverter},
                                    ConverterParameter=180}"
                                    MinWidth="200"
                                    SharedSizeGroup="Name"/>
                                <ColumnDefinition
                                    Width="55"
                                    MaxWidth="55"
                                    SharedSizeGroup="FileType"/>
                                <ColumnDefinition
                                    Width="50"
                                    MaxWidth="50"
                                    SharedSizeGroup="Size"/>
                            </Grid.ColumnDefinitions>
                            <Label Grid.Column="0"
                                   Grid.ColumnSpan="2"
                                   Content="文件名称"
                                   HorizontalAlignment="Left"
                                   VerticalAlignment="Center"
                                   Foreground="{TemplateBinding Foreground}"/>
                            <Label Grid.Column="2"
                                   Content="类型"
                                   HorizontalAlignment="Left"
                                   VerticalAlignment="Center"
                                   Foreground="{TemplateBinding Foreground}"/>
                            <Label Grid.Column="3"
                                   Content="大小"
                                   HorizontalAlignment="Center"
                                   VerticalAlignment="Center"
                                   Foreground="{TemplateBinding Foreground}"/>
                        </Grid>
                        <ScrollViewer Grid.Row="1"
                                      Grid.ColumnSpan="4"
                                      VerticalScrollBarVisibility="Auto"
                                      HorizontalScrollBarVisibility="Disabled">
                            <ItemsPresenter/>
                        </ScrollViewer>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>