<views:ExtenderAppView x:Class="ExtenderApp.Medai.MedaiMainView"
                       xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                       xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                       xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                       xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                       xmlns:local="clr-namespace:ExtenderApp.Medai"
                       xmlns:views="clr-namespace:ExtenderApp.Views;assembly=ExtenderApp.Views"
                       mc:Ignorable="d"
                       DataContext="{x:Type local:MedaiMainViewModel}"
                       d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition x:Name="videoGrid" Width="{Binding VideoListWidth}"/>
            </Grid.ColumnDefinitions>
            <!--视频播放区-->
            <!--<MediaElement x:Name="mediaElement"
            Grid.Row="0"
            LoadedBehavior="Play" />-->
            <ContentControl x:Name="playbackViewControl"/>
            <!-- 视频列表区域 -->
            <Border Grid.Column="1" >
                <ScrollViewer >
                    <ListBox x:Name="videoListBox" Background="Black"/>
                </ScrollViewer>
            </Border>
        </Grid>
        <Grid Panel.ZIndex="1" Height="20" VerticalAlignment="Bottom" Background="Transparent">
            <!--视频控制区-->
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Button Content="播放" Command="{Binding PlayCommand}"/>
                <Button Content="暂停" Command="{Binding PauseCommand}"/>
                <Button Content="停止" Command="{Binding StopCommand}"/>
                <Button Content="快进10秒" Command="{Binding FastForwardCommand}"/>
            </StackPanel>
        </Grid>
    </Grid>
</views:ExtenderAppView>
